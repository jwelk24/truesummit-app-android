<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug only. targetSdk 34 blocks cleartext HTTP by default, which would stop
  the app from reaching a backend running on the developer's machine over
  http://. Cleartext is permitted for the loopback addresses only; every other
  host still requires HTTPS, and release builds carry no cleartext allowance
  at all because this file is not part of that source set.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- The host machine, as seen from the Android emulator. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
